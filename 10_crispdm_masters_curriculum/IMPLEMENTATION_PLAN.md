# Implementation Plan

1. Read and preserve the supplied prompt.
2. Define the core task: Integrated CRISP-DM workflow.
3. Use the dataset/theme: Breast Cancer Wisconsin (scikit-learn).
4. Build the smallest experiment that demonstrates the requested concept.
5. Set `SEED = 42` for reproducibility.
6. Keep evaluation data separate where supervised evaluation is used.
7. Save metrics to JSON and one meaningful plot to PNG.
8. Generate a simple HTML dashboard.
9. Run the audit checks in `AUDIT_REPORT.md`.
10. Capture a browser screenshot and update the README.
