# Prompt Used

/teamwork-preview build an end to end data science project using CRISP-DM. Include quizzes, EDA, preprocessing, clustering, anomaly detection, supervised ML, associative rule mining, sub-linear search and synthesis.

## Reproduction decision

I kept the main learning objective but used a small, deterministic offline dataset or fallback when the original stack would be too heavy for a normal laptop.
