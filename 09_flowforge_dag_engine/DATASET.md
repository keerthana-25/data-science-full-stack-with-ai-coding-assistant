# Dataset

**Reference dataset/theme:** Synthetic pipeline graph

For reproducibility in this submission, the experiment uses either a built-in scikit-learn dataset or a deterministic synthetic dataset. This avoids requiring Kaggle credentials or network access during grading. Where the original project depends on external data, the README explains how the lightweight version maps to the original task.
